# 贡献指南

感谢你对自律助手项目的关注！我们欢迎所有形式的贡献。

## 🚀 如何贡献

### 报告Bug

如果你发现了bug，请[创建一个Issue](../../issues/new)并包含以下信息：

- **问题描述**：清晰描述遇到的问题
- **复现步骤**：如何重现这个问题
- **预期行为**：你期望发生什么
- **实际行为**：实际发生了什么
- **环境信息**：
  - 操作系统（Windows 10/11, macOS, Linux）
  - Flutter版本（运行`flutter --version`）
  - 应用版本

### 提出新功能

如果你有新功能建议，请[创建一个Issue](../../issues/new)并说明：

- **功能描述**：详细描述你想要的功能
- **使用场景**：这个功能解决什么问题
- **期望实现**：你期望它如何工作

### 提交代码

1. **Fork此仓库**

2. **创建特性分支**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **提交你的更改**
   ```bash
   git commit -m 'Add some amazing feature'
   ```

4. **推送到分支**
   ```bash
   git push origin feature/amazing-feature
   ```

5. **创建Pull Request**

## 📝 代码规范

### Dart代码风格

- 遵循[Dart官方代码风格](https://dart.dev/guides/language/effective-dart/style)
- 使用`flutter format`格式化代码
- 使用`flutter analyze`检查代码质量

### 提交信息规范

使用清晰的提交信息：

```
feat: 添加导入导出功能
fix: 修复日程重复显示的问题
docs: 更新README文档
style: 代码格式化
refactor: 重构规则引擎
test: 添加单元测试
chore: 更新依赖版本
```

### 文件组织

- **models/**: 数据模型，纯Dart类
- **services/**: 业务逻辑，不包含UI
- **screens/**: 页面组件
- **widgets/**: 可复用的UI组件
- **utils/**: 工具函数

## 🧪 测试

在提交PR之前，请确保：

1. 代码能够正常编译
   ```bash
   flutter build windows --debug
   ```

2. 没有分析警告
   ```bash
   flutter analyze
   ```

3. 格式化代码
   ```bash
   flutter format lib/
   ```

4. 测试核心功能
   - 创建规则
   - 导入导出
   - AI对话
   - 日程覆盖

## 🏗️ 开发环境设置

1. **克隆仓库**
   ```bash
   git clone https://github.com/your-username/self-discipline-assistant.git
   cd self-discipline-assistant
   ```

2. **安装依赖**
   ```bash
   flutter pub get
   ```

3. **配置API密钥**
   ```bash
   cp lib/config/api_keys.dart.template lib/config/api_keys.dart
   # 编辑 api_keys.dart 填入实际密钥
   ```

4. **运行应用**
   ```bash
   flutter run -d windows
   ```

## 🎯 优先级

我们特别欢迎以下方向的贡献：

- **高优先级**：
  - Bug修复
  - 性能优化
  - 文档改进
  
- **中优先级**：
  - 新功能开发
  - UI/UX改进
  - 单元测试
  
- **低优先级**：
  - 代码重构
  - 依赖更新

## ❓ 问题？

如有任何问题，欢迎：

- [创建Issue](../../issues/new)
- 在PR中提问
- 查看[现有Issue](../../issues)

## 📜 许可证

通过贡献代码，你同意你的贡献将遵循MIT许可证。
